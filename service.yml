apiVersion: v1
kind: Service
metadata:
  name: my-service
spec:
  ports:
    - protocol: TCP
      port: 5050
      targetPort: 5050
      nodePort: 30008
  type: NodePort
  selector:
    app: k8s-app
